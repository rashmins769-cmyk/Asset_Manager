version: '3.8'

services:
  assetlink-api:
    build: 
      context: .
      dockerfile: Dockerfile
    image: assetlink-api:latest
    container_name: assetlink-backend
    ports:
      - "8000:8000"
